{
  "name": "email-forwarder",
  "version": "0.1.0",
  "description": "Minimal Resend inbound email forwarding service deployed on Vercel.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/handle-webhook.test.ts"
  },
  "dependencies": {
    "resend": "^6.28.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
